---
import Layout, { Complexity } from "../layouts/Layout.astro";
const abc:Complexity = {expression: "",color:""}
---

<Layout title="Algorithm Visualizer" best={abc} worst={abc} average={abc} space={abc}>
	This algorithm visualizer is made by me, ChechuDEV.<br>
    It unfortunately runs with Javascript, which is kind of slow, so exceding 500 elements is not worth. 
</Layout>